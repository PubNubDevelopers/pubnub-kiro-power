{
  "mcpServers": {
    "pubnub": {
      "command": "npx",
      "args": ["-y", "@pubnub/mcp@latest"],
      "env": {
        "PUBNUB_API_KEY": "${PUBNUB_API_KEY}",
        "PUBNUB_USER_ID": "${PUBNUB_USER_ID}"
      }
    }
  }
}
